-- plugin/lvim-space.lua
-- Auto-loaded by Neovim when the plugin directory is on the runtimepath.
-- Prevents double-loading; actual initialisation is done via M.setup() in init.lua.
if vim.g.loaded_lvim_space then
    return
end
vim.g.loaded_lvim_space = true
